__pycache__/
*.pyc

# Training/calibration data — lives in github.com/Real-Coltix/Datasets, not here.
# datasets/ is ~15G; calib/ is 353M of derived .npy tensors, regenerated by
# build_calib.py (deterministic for a given --seed) once datasets/ is present.
datasets/
calib/

# local venvs
.venv/
